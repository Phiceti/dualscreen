/**
 * `dualscreen` — the all-in-one entry point.
 *
 * This root export is framework-agnostic and carries no React dependency.
 * React bindings live at `dualscreen/react`, display handling at
 * `dualscreen/screens`, and the debug overlay at `dualscreen/devtools`.
 */
export * from '@dualscreen/core'
